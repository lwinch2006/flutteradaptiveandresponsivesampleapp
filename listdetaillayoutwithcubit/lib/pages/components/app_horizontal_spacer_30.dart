import 'package:flutter/material.dart';

class AppHorizontalSpacer30 extends StatelessWidget {
  const AppHorizontalSpacer30();

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 30.0,
    );
  }
}
