import 'package:flutter/material.dart';

class AppVerticalSpacer30 extends StatelessWidget {
  const AppVerticalSpacer30();

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 30.0,
    );
  }
}
