import 'package:flutter/material.dart';

class AppVerticalSpacer20 extends StatelessWidget {
  const AppVerticalSpacer20();

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20.0,
    );
  }
}
