import 'package:flutter/material.dart';

class AppHorizontalSpacer20 extends StatelessWidget {
  const AppHorizontalSpacer20();

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 20.0,
    );
  }
}
